
 ✓ Allocatie
 ? Hoare types
 W Hoare logic
    W sequence
    W theorems mbt mutation operaties
 ? While
 R Denotational semantics
    R equivalentie met small step
 ? Normalisation
 ? Type safety (progress)


Niet:
 - big step
 - fixpoints
 - separation
